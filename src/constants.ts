// these values are arbitrary, you can decide your own
const authorizationCodeDuration: number = 1800 // 30 minutes
const accessTokenDuration: number = 86400 // 24 hours

export { accessTokenDuration, authorizationCodeDuration }
